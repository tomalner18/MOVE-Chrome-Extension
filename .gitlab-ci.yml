test:
  stage: test
  script:
    - apt-get update -y
    - apt-get install -y python3
    - pytho3 test/MissingFileTest.py

deploy:
  stage: deploy
  only: master
  script:
    - apt-get update -y
    - apt-get install -y zip
    - rm extension.zip
    - zip -r extension.zip
    - git commit -a -m "New Release of Extension"
    - git push


